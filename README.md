MyPortfolio

Local development:

1. Install dependencies (Node.js and npm required)

```bash
npm install
```

2. Start the dev server:

```bash
npm run dev
```

This uses `live-server` via `npx` and will open `index.html` in your browser.

GitHub Pages: https://jayrhuuu.github.io/MyPortfolio/
